"""
app/person_detector.py — placeholder (YOLO disabled)

YOLOv8n person detection was planned but disabled to avoid
long image rebuild times. verify3.py now uses direct zone
crop instead.
"""
# This module is intentionally left empty.
# Re-enable when ultralytics is added back to requirements.txt
